
file: Interpreter: public class Interpreter:
	
	private static ApplicationCommands Parse (string line):
		
		line format: Command method [-options[:args]] [arguments]
		
		Command:
		
			Population, pop: new
			
			Application, app: reset, quit
		
file: Option: public struct Option:

	public Option (string name, string[] arguments)
	
file: ApplicationCommands: public enum ApplicationCommands:

	commands to pass back to entry point from interpreter.
	
	{ Continue, Reset, Quit }
	
file: PopulationControl: public class PopulationControl

	interfaces console access to Population
	
	static members only
	
	public static void New (Option[] options, string[] arguments):
	
		Options:
		
			-size, s:(x)[:(y) (...) ]
			-rule, r:[(k),(n)]  // literalbrackets
			-variety, v:(variety)
		
		Arguments: (target) [(intial conditions)]
	